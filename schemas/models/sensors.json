{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The sensor unique identifier"
      },
      "temperature": {
        "type": "integer",
        "description": "Temperature value (e.g., '21')"
      },
      "temperature_unit": {
        "type": "string",
        "description": "Temperature unit ('°C': degree celsius,'°F': degree fahrenheit)",
        "enum": ["°C", "°F"]
      },
      "humidity": {
        "type": "integer",
        "description": "Humidity level in percent (optional, e.g., '60')"
      },
      "pressure": {
        "type": "integer",
        "description": "Pressure in hectopascals (optional, e.g., '1038')"
      },
      "room": {
        "type": "string",
        "description": "The sensor room"
      },
      "floor": {
        "type": "string",
        "description": "The sensor floor"
      }
    },
    "required": ["id", "temperature", "temperature_unit", "room"]
  }
}