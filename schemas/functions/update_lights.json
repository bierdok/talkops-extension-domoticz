{
  "name": "update_lights",
  "description": "Update lights",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["On", "Off"],
        "description": "The desired action"
      },
      "ids": {
        "type": "array",
        "description": "The light identifiers",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["action", "ids"]
  }
}
