{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique identifier"
    },
    "name": {
      "type": "string",
      "description": "The name"
    },
    "state": {
      "type": "string",
      "enum": ["enabled", "disabled"],
      "description": "The current state (optional)"
    }
  },
  "required": ["id", "name"]
}
