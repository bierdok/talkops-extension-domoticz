{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "integer",
        "description": "The scene unique identifier"
      },
      "name": {
        "type": "string",
        "description": "The scene name"
      },
      "description": {
        "type": "string",
        "description": "The scene description"
      },
      "state": {
        "type": "string",
        "enum": ["enabled", "disabled", null],
        "description": "The current scene state (optional)"
      }
    },
    "required": ["id", "name"]
  }
}